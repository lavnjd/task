#version check
git --version
#set user name
git config --global user.name "lavanya"
#set email
git config --global user.email lavanyatutika123@gmail.com
#editor to wait
git config --global core.editor "code --wait"
#to run editor 
git config --global -e

